class ArticleModel {
  String author, title, description, url, urlToImage, content;

  ArticleModel({
    this.author,
    this.title,
    this.description,
    this.url,
    this.content,
    this.urlToImage,
  });
}
